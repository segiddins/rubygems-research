<%= form_with(model: rubygem) do |form| %>
  <% if rubygem.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rubygem.errors.count, "error") %> prohibited this rubygem from being saved:</h2>

      <ul>
        <% rubygem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :server_id, style: "display: block" %>
    <%= form.text_field :server_id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
